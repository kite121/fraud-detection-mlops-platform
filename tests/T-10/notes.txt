Test ID: T-10
Test Name: Auto-monitoring trigger after POST /ingest
Date: 26.04.26
Tester: Amir Valeev
Branch: fixed_final_branch
Environment: Local (Windows 11)

Goal:
- Verify that ingesting a new batch automatically triggers the monitoring flow through the data_ingested event.

Preconditions:
- The platform is running in Docker.
- A best model already exists in the registry, so the listener can compare a new batch against the current reference batch.
- Training API docs are available at http://localhost:8000/docs.

Steps performed:
1. Uploaded a new batch through POST /ingest.
2. Collected recent ingestion-service logs after the upload.
3. Checked that the service logs showed automatic monitoring behavior after the data_ingested event.

Expected result:
- The new batch is ingested successfully.
- After ingest, the training-side listener automatically runs drift monitoring.
- If drift is below threshold, the service logs state that no retraining is needed.

Actual result:
- POST /ingest completed successfully and created batch_id 10.
- The ingestion-service logs showed the new POST /ingest request and then logged: Drift below threshold; no retraining needed for batch 10.
- This confirms that auto-monitoring was triggered after ingest and completed without requiring retraining.

Key values / IDs:
- batch_id: 10
- job_id:
- model_version:
- mlflow_run_id:
- storage_path: s3://raw-data/clients/bank_test/raw/v1/2026-04-26/tmpq552cs6_.csv
- model_path:
- metrics_path:
- reference_profile_path:
- drift_result_path:

Evidence collected:
- outputs.txt
- screenshots/auto-monitoring-ingest-response.png
- screenshots/training-service-auto-monitoring-logs.png

Status:
- PASS

Notes / Issues:
- The GET /metrics lines in the logs were generated by Prometheus scraping and are not errors.
- The test outcome is correct for a repeated dataset: auto-monitoring ran, drift stayed below threshold, and retraining was not triggered.
